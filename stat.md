# Se agregó el botón "Netstat" en la interfaz.
# Se implementó el método ejecutarNetstat() que corre los comandos netstat -a, -n y -o.
# Se añadió un panel lateral con JTextArea para mostrar la salida de netstat.
